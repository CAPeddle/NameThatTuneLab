<resources>
    <!-- Base application theme — Compose-driven, so this is minimal -->
    <style name="Theme.NameThatTuneLab" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
    </style>
</resources>
